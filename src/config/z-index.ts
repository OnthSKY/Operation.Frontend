export const Z_INDEX = {
  navbar: 40,
  /** Full-screen mobile hub below sidebar so the slide-out can stack above when opened from it */
  mobileFullMenu: 45,
  sidebar: 50,
  drawer: 60,
  modal: 70,
} as const;
